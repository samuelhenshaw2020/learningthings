import { ScrollProdDirective } from './scroll-prod.directive';

describe('ScrollProdDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollProdDirective();
    expect(directive).toBeTruthy();
  });
});
